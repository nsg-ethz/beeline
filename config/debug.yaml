hosts:
- name: server1
  address: 127.0.0.1
- name: server2
  address: 127.0.0.1
spec:   
  http:
    # parse:
    # - match:
    #     backend: server1
    #     backend: server2
    #   extract:
    #     content-length: u32
  - match:
      backend: server1
    headers:
      request:
        add:
          source: ebpf
      response:
        add:
          test: performance
        remove:
          - date
          - develop
    route:
    - destination:
        host: server1
        port: 8000
  - match:
      backend: server2
    headers:
      response:
        add:
          test: performance
        remove:
          - asdf
    route:
    - destination:
        host: server2
        port: 8000