hosts:
    - name: server1
      instances:
          - 127.0.0.1:8000
    - name: server2
      instances:
          - 127.0.0.1:8002

parse:
    http:
        backend: str,
        content-length: u32
